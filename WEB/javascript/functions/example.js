// below is the some simple function, an example of how javascript works



// you see , functions in js are declared just like variables, 
//no need to specify the return value
var greeting = function(name){

	alert("Hello " + name);
}

greeting("Rafael");